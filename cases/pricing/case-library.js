// Pricing case index note:
// The pricing case library is intentionally empty while the case logic is
// being restructured. Keep this file as the public index contract so the
// pricing library page can render safely before new records are added.
window.pricingCaseRecords = [];
